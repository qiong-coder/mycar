<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycar.mapper.OrderMapper">

    <insert id="insertOrder" parameterType="com.mycar.model.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO Order (vid, begin, end, rent_address, return_address, name, identity, phone, bill)
        VALUES (#{vid}, #{begin}, #{end}, #{rent_address}, #{return_address}, #{name}, #{identity}, #{phone}, #{bill})
    </insert>

</mapper>