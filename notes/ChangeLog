	2017-11-10:
	1.车型信息，加入默认价格的设定，现在都默认是100元/天。
	2.车型信息，加入“油品标号_______号以上”。
	3.车型信息，合并价格日历和优惠日历，显示原价、折扣、现价三个信息。
	4.添加修改订单接口(包括车型),仅修改订单内容但不改变当前订单状态。
	5.取车(受理)时车型可修改。
	6.取车(受理)时拥有最高权限，可以选择所有在库的车型、车牌（包括预留）。预留的概念，仅仅对于互联网订车有效一旦发生冲突，提示用户冲突，用户可以去修改哪些互联网订单（待受理），改变车型、租期。
	7.添加查询订单冲突接口(修改车型及时间时调用, 用于提醒操作员)。
	8.证件号码可以为非身份证

	--------------------------------------------------
	1. 在添加VehicleInfo结构体的时候，增加一个可选cost字段，该字段为JSON结构体，必须包含如下字段，{base_insurance:基础保险，free_insurance:免责保险，day_cost:日用车费用，discount:打折}
	2. VehicleInfo请求添加车型信息时候，在请求结果体增加一个字段oil，表示油品标号
	3. 在获取车型信息接口，会返回对应的字段
	4. 增加接口(http://ip:port/mycar/order/{oid}/ ,method=PUT),满足修改信息，但是不会改变订单状态
	5. 取车接口，可以提交非订单约定的车型的车牌号，后台自动更新订单的车型ID
	6-7. 增加接口(http://ip:port/mycar/order/conflict/{viid}/{begin}/{end}/, method=GET)接口，用来计算车型viid的车辆，未来的使用情况。其中返回结构中会包含：total总共该车型有多少辆车，used：已经租出去多少（这里是提车了多少），to_used：表示还有多少辆车需要在时间段内被租出去，orders包含了未来时间段内需要提车的租车订单情况。
	8. 在原有接口增加逻辑，如果输入为身份证号，则采取非层存储的方法，否则直接存储
